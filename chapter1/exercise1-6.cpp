#include <iostream>

/* You want to read the numbers of just the As. */

int main()
{
    std::cout << "This will still take O(n) despite only needing to search 1/26th of the phone book." << std::endl;
}
